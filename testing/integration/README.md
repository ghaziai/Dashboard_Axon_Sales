# Uji Integrasi

Alur lintas fitur (misalnya siklus penuh CRUD dengan Supabase, atau filter dasbor → kueri → rendering) dijalankan terutama pada branch `Testing` sebelum rilis dipromosikan ke `Deployment`.

**Status:** Belum diimplementasikan — menunggu koneksi Supabase dan fitur CRUD pertama.